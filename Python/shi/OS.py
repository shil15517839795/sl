#!/usr/bin/python
# -*-coding:utf-8-*-
# import os
# a = os.getcwd()#显示当前文件所在路径
# print(a)
# print(os.name)#打印结果为nt 代表Windows系统
# list=os.listdir(a)#返回指定路径下的所有文件和目录名，括号里可以填相对路径或者绝对路径，如果括号里不填则默认为当前路径
# for i in list:
#     if i.endswith('xls'):
#         delete=os.remove(i)
# print(list)
# delete=os.remove(r'E:\shi\Python\shi.py')#删除指定路径下的文件，括号里可以填文件的路径
# delete_dir=os.removedirs(r'qq\ww\ee')#删除指定路径下的空目录，删除递归目录
# delete_dir=os.rmdir()#用来删除空的单级目录
# dir=os.mkdir('www')#创建单机目录
# dirs=os.makedirs(r'11\22\33\44')#创建递归目录
# a=os.path.isfile('www')#判断指定路径下的名称是否是文件，是的话就打印True、不是就打印False
# print(a)
# b=os.path.isdir('b.txt')#判断指定路径下的名称是否是目录，是的话就打印True、不是就打印False
# print(b)


# c=os.path.split(r'E:\shi\Python\shi\www')#将路径分割成路径名和文件名
# print(c)
# cc=os.path.join(r'E:\shi\Python\shi\www','dfg.py')#将路径和文件名组合在一起
# print(cc)

# i=os.path.exists('b.txt')#判断括号里的文件或者目录是否存在，存在就打印True，不存在就打印False
# print(i)

